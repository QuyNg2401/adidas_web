# Frontend-only (Mock)

Chạy UI độc lập, không cần backend/database.

## Dev

```bash
npm install
npm run dev
```

## Deploy Vercel
- Root Directory: `frontend-only`
- Framework: Vite
- Build Command: `npm run build`
- Output: `dist`
